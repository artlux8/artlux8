// GeoRedirectBanner is no longer needed - UK users are auto-redirected in localizationStore
// This component is kept for backwards compatibility but renders nothing

const GeoRedirectBanner = () => {
  return null;
};

export default GeoRedirectBanner;
